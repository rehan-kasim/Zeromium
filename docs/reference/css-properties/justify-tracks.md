# CSS property: justify-tracks

Zeromium `zerocss` implementation note for **justify-tracks**.

- **Spec**: CSS Living Standard / relevant module.
- **Parsing**: tokenize in `zerocss` tokenizer; validate grammar.
- **Cascade**: participates in origin/specificity/layer order.
- **Computed value**: resolved by `zerocss` (em/rem/%, var(), calc()).
- **Used by**: `zerolayout` (geometry) and `zerorender` (paint).
- **Inheritance**: see spec; `zerocss` stores initial/inherit rules.
- **Conformance**: WPT `css/` suite.

## Checklist
- [ ] Grammar parse
- [ ] Cascade priority
- [ ] Computed-value resolution
- [ ] Layout effect
- [ ] Paint effect

