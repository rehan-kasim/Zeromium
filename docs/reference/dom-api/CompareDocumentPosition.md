# DOM API: CompareDocumentPosition

Zeromium `zerodom` + `hyperion-js` binding note for **CompareDocumentPosition**.

- **Interface**: `CompareDocumentPosition` (spec-defined surface).
- **Storage**: node handles are `NodeId` in a `zeromem` arena.
- **Binding**: exposed to `hyperion-js` via generated glue.
- **Security**: cross-origin access gated by `zerosec`.
- **Conformance**: WPT `dom/` and `domparsing/`.

## Checklist
- [ ] Interface shape
- [ ] Behavior per spec
- [ ] Event integration
- [ ] JS binding
- [ ] Origin checks

