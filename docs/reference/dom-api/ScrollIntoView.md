# DOM API: ScrollIntoView

Zeromium `zerodom` + `hyperion-js` binding note for **ScrollIntoView**.

- **Interface**: `ScrollIntoView` (spec-defined surface).
- **Storage**: node handles are `NodeId` in a `zeromem` arena.
- **Binding**: exposed to `hyperion-js` via generated glue.
- **Security**: cross-origin access gated by `zerosec`.
- **Conformance**: WPT `dom/` and `domparsing/`.

## Checklist
- [ ] Interface shape
- [ ] Behavior per spec
- [ ] Event integration
- [ ] JS binding
- [ ] Origin checks

